import assert from 'node:assert/strict';
import { buildEpubBookSectionModel } from '../src/lib/epub/bookSectionModel.js';
import { activateReaderModel, normalizeReaderModelMode } from '../src/lib/epub/readerModelActivation.js';
const packageModel={manifest:new Map(),guideReferences:[],coverCandidates:[],spine:[0,1,2].map(spineIndex=>({spineIndex,resource:{canonicalHref:['cover.xhtml','list.xhtml','chapter.xhtml'][spineIndex]}})),navigation:[{index:0,title:'Arbitrary label',target:{documentHref:'list.xhtml',fragmentId:null}},{index:1,title:'Chapter One',target:{documentHref:'chapter.xhtml',fragmentId:null}}]};
const documents=[{documentHref:'cover.xhtml',spineIndex:0,navigationProfile:{isNavigationList:false},events:[{type:'image',eventIndex:0,imageHref:'cover.jpg',alt:''}]},{documentHref:'list.xhtml',spineIndex:1,navigationProfile:{isNavigationList:true},events:[{type:'text-block',eventIndex:0,plainText:'Arbitrary navigation label'}]},{documentHref:'chapter.xhtml',spineIndex:2,navigationProfile:{isNavigationList:false},events:[{type:'text-block',eventIndex:0,plainText:'第一文。第二文。'},{type:'image',eventIndex:1,imageHref:'ornament.png',alt:''},{type:'image',eventIndex:2,imageHref:'inline.jpg',alt:''},{type:'text-block',eventIndex:3,plainText:'第三文。'}]}];
const bookModel=buildEpubBookSectionModel({packageModel,documents});assert.equal(bookModel.sections.some(s=>s.type==='navigation'&&s.start.spineIndex===1),true);assert.equal(bookModel.sections.filter(s=>s.type==='chapter').length,1);assert.equal(bookModel.sections.find(s=>s.type==='chapter').start.spineIndex,2);assert.equal(JSON.stringify(bookModel).includes('Arbitrary navigation label'),false);
const runtime={packageModel,documents,bookModel:{...bookModel,cover:{chosen:{documentHref:'cover.xhtml',eventIndex:0}}},imageModel:{occurrences:[{documentHref:'cover.xhtml',eventIndex:0,candidateRole:'cover',confidence:'high'},{documentHref:'chapter.xhtml',eventIndex:1,candidateRole:'ornament',confidence:'medium'},{documentHref:'chapter.xhtml',eventIndex:2,candidateRole:'inline-illustration',confidence:'high'}]},diagnostics:{reconstructionFailureCount:0,documents:documents.map(()=>({unownedTextNodeCount:0,duplicateTextNodeCount:0}))}};const zip={file:href=>({async:async()=>new Blob([href])})};const legacy={flatItems:[{type:'sentence',plainText:'legacy'}],chapters:[{id:'legacy'}],chapterImageLists:{0:[]}};assert.equal(normalizeReaderModelMode('bad'),'legacy');const compare=await activateReaderModel({mode:'shadow-compare',legacy,runtime,zip});assert.equal(compare.flatItems,legacy.flatItems);const active=await activateReaderModel({mode:'qualified-shadow',legacy,runtime,zip});assert.equal(active.activation.activated,true);assert.equal(active.flatItems.some(x=>x.plainText==='Arbitrary navigation label'),false);assert.equal(active.flatItems.some(x=>x.parserDebug?.imageRole==='ornament'),false);const image=active.flatItems.find(x=>x.parserDebug?.imageRole==='inline-illustration');assert.ok(image);assert.equal(active.chapterImageLists[0][0],image);assert.equal(image.parserDebug.sceneIndex,active.flatItems.indexOf(image));assert.deepEqual(active.flatItems.filter(x=>x.type==='sentence').map(x=>x.plainText),['第一文。','第二文。','第三文。']);const broken={...runtime,diagnostics:{reconstructionFailureCount:1,documents:[]}};const fallback=await activateReaderModel({mode:'qualified-shadow',legacy,runtime:broken,zip});assert.equal(fallback.activation.fallbackReason,'reconstruction-failure');console.log('Phase 13.6A structural Reader activation tests passed');
